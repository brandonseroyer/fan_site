<%= render('layouts/errors', :object => @artist) %>

<%= form_for [@genre, @artist] do |f| %>
<%= f.label :name %>
<%= f.text_field :name %>
<%= f.label :review %>
<%= f.text_field :review %>
<p><%= f.submit %></p>

<% end %>
